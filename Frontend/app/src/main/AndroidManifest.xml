<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.navigation_screen">

    <!-- Permission to access the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo1"
        android:supportsRtl="true"
        android:theme="@style/Theme.Navigation_screen"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">




        <!-- MainActivity set as the launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity declaration without intent-filter -->
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="@string/app_name">
            <!-- Removed intent-filter for Login activity -->
        </activity>

        <!-- SignUp Activity declaration without intent-filter -->
        <activity
            android:name=".SignUp"
            android:exported="true"
            android:label="@string/app_name">
            <!-- Removed intent-filter for SignUp activity -->
        </activity>

        <activity
            android:name=".RateCourse"
            android:exported="true"
            android:label="@string/app_name">
            <!-- Removed intent-filter for SignUp activity -->
        </activity>

        <activity android:name=".WorkloadGauge">

        </activity>
        <activity android:name=".HomeAdmin">

        </activity>

    </application>
</manifest>
